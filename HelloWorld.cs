using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //etokaroche gavarit hello world
        
    }

    // Update is called once per frame
    void Update()
    {
        print("hello_world");
    }
}
